Ext.define('Hospital.view.pacient.ViewController', {
    extend: 'Ext.app.ViewController',
    alias: 'controller.pacient-view',

    /*onAddClick: function()
    {
    	var view = this.getView();
        var emptyData = Ext.create('Hospital.model.Pacient', {});
    	emptyData.save();
    	view.refresh();
    },*/
    
});
